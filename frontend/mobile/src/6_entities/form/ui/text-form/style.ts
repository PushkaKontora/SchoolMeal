import {StyleSheet} from 'react-native';
import {TextFormProps} from './props';

export const createStyle = (props: TextFormProps) => StyleSheet.create({
  input: {
    // marginTop: 4,
    // fontWeight: '400',
    // fontSize: 11,
    // lineHeight: 13,
    // color: props?.displayErrorMessage == true ? '#FFFFFF' : '#EE6725',
  },
});
