version: "3.9"

services:
  postgres:
    ports:
      - ${DB_PORT:-5432}:${DB_PORT:-5432}

  app:
    ports:
      - "8000:8000"
    environment:
      SHOW_SWAGGER_UI: "true"
    volumes:
      - ./backend/app:/school_meal/app

  nginx:
    build:
      target: proxy
    environment:
      ORIGIN: "*"
    volumes:
      - ./frontend/web/nginx:/etc/nginx/templates